export const metadata = {
  title: "FlxStore",
  description: "Minha loja",
};

export default function RootLayout({ children }) {
  return (
    <html lang="pt-BR">
      <body>{children}</body>
    </html>
  );
}
